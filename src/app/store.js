import { configureStore } from "@reduxjs/toolkit";

//calling configure store as a function and passing object inside
export default configureStore({
    reducer :{} //Leaving the reducer parameter as empty object for now

})